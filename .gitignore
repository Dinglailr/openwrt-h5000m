*.o
.DS_Store
.*.swp
/env
/dl
# /.config intentionally tracked — single source of truth for H5000M package list
/.config.old
/.toolchain_build_ver
/bin
/build_dir
/staging_dir
/tmp
/logs
/feeds
/feeds.conf
# /files is intentional build overlay — do NOT ignore it
/target/linux/feeds
/overlay
/package/feeds
/package/openwrt-packages
/*.patch
/llvm-bpf*
key-build*
private-key.pem
public-key.pem
*.orig
*.rej
*~
.#*
*#
.emacs.desktop*
TAGS*~
git-src
.project
.cproject
.ccache
.vscode*
firmware_out/
monitor.sh
notify_build.sh
notify_router.sh
start_build.sh

# Build products — one canonical output directory, ignored.
# Three near-identical firmware dirs (firmware-output/, firmware_output/,
# firmware_out/) had accumulated 142 MB of superseded images, plus a 337 MB
# second clone of this very repo. They were untracked, so nothing warned.
/firmware-out/
/firmware-output/
/firmware_output/
/firmware_out/
/local-build-data/
/firmware_ready_to_flash.bin
/jq-linux-*
/.build_status
/build.log
